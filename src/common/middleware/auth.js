'use strict';
/**
 * middleware
 */
export default class extends think.middleware.base {
  /**
   * run
   * @return {} []
   */
  run(){
    // if(this.session.user){
    // 	this.next();
    // }else{
    	
    // }
  }
}